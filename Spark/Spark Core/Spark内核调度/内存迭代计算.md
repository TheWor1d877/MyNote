## 内存迭代计算
在stage的内部，一定都是窄依赖
![[Attachments/Pasted image 20250829142518.png]]
- 在同一个阶段中由3个线程并行工作
- 阶段之间可能需要走网络
- 优先保留并行度，然后是内存计算管道 => 宁可使用网络也需要将并行度拉满
- 一个阶段内部是窄依赖，窄依赖内部如果前后形成1：1的分区对应关系，就可以产生许多内存呢迭代计算的管道
- 内存迭代管道就是Task

## 与mareduce比较
- spark的算子多
- 使用内存进行计算而不是硬盘
