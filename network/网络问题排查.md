## 节点A无法连接节点B的6800端口（Ceph OSD通信端口）。
1. 检查端口是否正常监听
```bash
sudo ss -tulnp | grep :6800
```
2. 在节点A上面测试联通性
```bash
ping nodeB_ip
```
3. 使用nc来测试联通性
```bash
nc -v nodeB_ip 6800
```
4. 如果失败检查防火墙
```bash
sudo ufw status
```
5. 检查路由
```bash
mtr -n nodeB_ip
```