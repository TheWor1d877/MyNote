## 三次握手
作用：
1. 防止历史连接被错误的建立
客户端发送SYN，但是对方没有收到，如果只有两次握手，那就客户端确认第二次握手，发送数据，但是客户端发现这不是我需要的数据

2. 收发双方确认序列号
seq本质上不是从0开始的，而是一个随机数

3. 确认收发能力，WIN与WS 

## 四次挥手
两组挥手，双方分别向对方发送一个断开连接请求，然后对方确认
进入半关闭状态，单方不请求了
四次挥手也可能是三次或者两次之后RST

## 两个MSL
挥手之后进入TIME_WAIT
确保最后一个ACK对方收到，如果没有那么会重发FIN
防止旧数据突然再次收到
