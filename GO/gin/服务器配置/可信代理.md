```go
    // 【关键】设置可信代理列表
    // 这里填你的反向代理服务器（如 Nginx）或云负载均衡器的内网IP
    if err := r.SetTrustedProxies([]string{
        "192.168.1.0/24", // 信任整个内网网段
        "10.0.0.5",        // 信任特定IP
        "203.0.113.0/24",  // 信任CDN的IP段（需从CDN提供商处获取）
    }); err != nil {
        panic(err)
    }
```

