反射是程序在运行时动态探查类型和值的功能

编译期间只知道类型是interface{},运行时通过reflect.Typeof() 与 reflect.Valueof() 获取动态信息

在Go语言中反射的开销很大，原因：
1. 内存分配：每次reflect.Valueof()都会分配堆对象
2. 运行期间做类型检查，每次必查表
3. 热路径函数无法被编译器内联，如果在存储节点路径中 每条日志都使用反射，那么QPS就会下降

避免反射：编译期类型信息来替代，如zap.Int()

处理完全未知的数据的时候会用到反射
