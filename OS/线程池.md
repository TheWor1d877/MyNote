线程池（Thread Pool）是预先创建并管理一组线程，避免频繁创建销毁线程开销的技术，核心是 “用线程复用替代线程创建”。

CPU密集型	CPU核心数 ± 1	充分利用CPU，避免过多切换
IO密集型	CPU核心数 × 2~5	线程在IO阻塞时，其他线程可用CPU

线程池解决三个核心问题：1）线程创建销毁开销大；2）系统资源有限（线程数有上限）；3）管理复杂性高。它通过预先创建线程+任务队列，实现线程复用，提高响应速度，控制并发度，避免为每个任务‘零售式’创建线程，大幅提升性能稳定性,是高性能服务器的标配组件。

