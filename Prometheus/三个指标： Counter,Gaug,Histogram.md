Prometheus 的指标（metrics）本质是 “描述系统状态的数字”。它定义了三种最常用的类型：

## Counter(计数器)
特点：只增不减，从 0 开始一直往上加。
用途：统计“总量”
注意：如果服务重启，Counter 会归零重来（这是正常的，Prometheus 会处理这种重置）。

## Gauge(仪表盘)
特点：可增可减，表示当前瞬时值。
用途：描述“当前状态”。

## Histogram(直方图)
特点：记录一批数值的分布情况（比如延迟、文件大小）。
自动计算：平均值、P95、P99 等。

